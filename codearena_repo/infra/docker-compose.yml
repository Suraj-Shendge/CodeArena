version: '3'

services:

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: codearena
      POSTGRES_PASSWORD: codearena
      POSTGRES_DB: codearena
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"